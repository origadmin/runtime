app:
  id: "transformer-app-id"
  name: "OriginalApp"
  version: "1.0.0"
  env: "transformer-test"

sources:
  - type: file
    file:
      path: "config.yaml"
transformers:
  - type: "test-transformer"
    options:
      suffix: "-transformed"
