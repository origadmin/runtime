logger:
  level: debug
  format: text
  stdout: true

registration_discovery_name: "test-discovery"

grpc_servers:
  - network: "tcp"
    addr: ":9000"
    timeout: "1s"
http_servers:
  - network: "tcp"
    addr: ":8000"
    timeout: "2s"
