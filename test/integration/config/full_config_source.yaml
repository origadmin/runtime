clients:
    - user-service
    - stock-service
discoveries:
    - config:
        consul:
            address: consul.internal:8500
        name: my-test-app
        type: consul
      name: internal-consul
    - config:
        etcd:
            endpoints:
                - etcd.legacy:2379
        type: etcd
      name: legacy-etcd
grpc_servers:
    - addr: :9001
http_servers:
    - addr: :8001
registration_discovery_name: internal-consul
